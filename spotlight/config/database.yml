# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
#default: &default
#  adapter: mysql2
#  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#  timeout: 5000
#  # encoding: unicode
#  host: <%= ENV['MYSQL_HOST_APP'] || 'db' %>
#  port: <%= ENV['MYSQL_PORT'] || '3306' %>
#  username: <%= ENV['MYSQL_USER'] || 'spotlight_db_admin' %>
#  password: <%= ENV['MYSQL_PASSWORD'] || 'password' %>
#
#development:
#  <<: *default
#  database: <%= ENV['MYSQL_DB_APP'] || 'spotlight_development' %>
#
## Warning: The database defined as "test" will be erased and
## re-generated from your development database when you run "rake".
## Do not set this db to the same as development or production.
#test:
#  <<: *default
#  database: <%= ENV['MYSQL_DB_TEST'] || 'spotlight_test' %>
#
#production:
#  <<: *default
#  database: <%= ENV['MYSQL_DB_APP'] || 'spotlight_production' %>

default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  encoding: unicode
  host: <%= ENV['POSTGRES_HOST_APP'] || 'db' %>
  port: <%= ENV['POSTGRES_PORT'] || '5432' %>
  username: <%= ENV['POSTGRES_USER'] || 'postgres' %>
  password: <%= ENV['POSTGRES_PASSWORD'] || 'password' %>

development:
  <<: *default
  database: <%= ENV['POSTGRES_DB_APP'] || 'spotlight_development' %>

test:
  <<: *default
  database: <%= ENV['POSTGRES_DB_TEST'] || 'spotlight_test' %>

production:
  <<: *default
  database: <%= ENV['POSTGRES_DB_APP'] || 'spotlight_production' %>
  pool: 50